node_modules/
dist/
build/
.expo/
.env
.env.local
*.log
.DS_Store

# Prisma
apps/api/prisma/dev.db

# ไฟล์ที่อัปโหลดผ่าน POST /api/uploads (รูปยืนยันตัวตนคนขับ ฯลฯ)
apps/api/uploads/

# IDE
.vscode/
.idea/

# เอกสาร proposal เก็บไว้ในเครื่องเท่านั้น ไม่ขึ้น repo (public)
Proposal-*.pdf

# กุญแจลับ Firebase Admin (service account) — ห้ามขึ้น repo เด็ดขาด ใครได้ไปส่ง push ในนามแอปได้
*firebase-adminsdk*.json
*service-account*.json
google-services.json
